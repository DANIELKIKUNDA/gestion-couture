import { PERMISSIONS } from "./permissions.js";
import { ROLES } from "./roles.js";

export const DEFAULT_ROLE_PERMISSIONS = {
  [ROLES.PROPRIETAIRE]: Object.values(PERMISSIONS),
  [ROLES.ADMIN]: [
    PERMISSIONS.VOIR_BILANS_GLOBAUX,
    PERMISSIONS.MODIFIER_PARAMETRES,
    PERMISSIONS.GERER_UTILISATEURS,
    PERMISSIONS.CLOTURER_CAISSE,
    PERMISSIONS.TERMINER_COMMANDE,
    PERMISSIONS.LIVRER_COMMANDE,
    PERMISSIONS.ANNULER_COMMANDE
  ],
  [ROLES.CAISSIER]: [PERMISSIONS.CLOTURER_CAISSE],
  [ROLES.COUTURIER]: [PERMISSIONS.TERMINER_COMMANDE, PERMISSIONS.LIVRER_COMMANDE]
};
